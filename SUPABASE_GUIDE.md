# Supabase 설정 가이드

포트폴리오 데이터를 저장하고 관리하기 위해 Supabase 설정이 필요합니다. 아래 순서대로 진행해주세요.

## 1. API 키 및 URL 찾기 (가장 중요!)
1. [Supabase 대시보드](https://supabase.com/dashboard)에 접속하여 로그인합니다.
2. **New project**를 클릭하여 새 프로젝트를 생성합니다 (이름은 자유, 예: `my-portfolio`).
3. 프로젝트가 생성되면, 왼쪽 사이드바 하단의 **Settings (톱니바퀴 아이콘)**를 클릭합니다.
4. **API** 메뉴를 선택합니다.
5. 여기에 있는 두 가지 값이 필요합니다:
   - **Project URL**: 페이지 상단에 있는 URL (`https://...supabase.co`). `VITE_SUPABASE_URL` 값으로 사용
   - **Publishable key** (또는 `anon` `public`): `sb_publishable_...`로 시작하는 키. `VITE_SUPABASE_ANON_KEY` 값으로 사용
     *(주의: Secret key는 절대 사용하지 마세요!)*

## 2. 환경 변수 적용하기

### 로컬 개발 환경 (내 컴퓨터)
프로젝트 최상위 폴더에 `.env` 파일을 만들고 아래 내용을 채워넣으세요.
(주의: `.env` 파일은 깃허브에 올리지 마세요)

```env
VITE_SUPABASE_URL=여기에_Project_URL_붙여넣기
VITE_SUPABASE_ANON_KEY=여기에_anon_key_붙여넣기
VITE_ADMIN_PASSWORD=원하는_관리자_비밀번호_설정
```

### 배포 환경 (Netlify)
1. Netlify 대시보드에서 해당 사이트를 선택합니다.
2. **Site configuration** > **Environment variables** 메뉴로 이동합니다.
3. **Add a variable**을 클릭하여 위 3가지 변수(`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_ADMIN_PASSWORD`)와 값을 각각 추가합니다.
4. **Deploys** 탭으로 가서 **Trigger deploy** > **Deploy site**를 눌러 재배포합니다.

---

## 3. 데이터베이스 테이블 만들기
Supabase 대시보드 왼쪽 메뉴에서 **SQL Editor**를 클릭하고, 아래 코드를 복사해서 붙여넣은 뒤 **Run** 버튼을 누르세요.

```sql
-- 1. Projects 테이블 (포트폴리오 프로젝트)
CREATE TABLE projects (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  title TEXT NOT NULL,
  type TEXT NOT NULL,
  description TEXT,
  role TEXT,
  link TEXT,
  thumbnail TEXT,
  is_featured BOOLEAN DEFAULT FALSE,
  is_main BOOLEAN DEFAULT FALSE,
  category TEXT NOT NULL,
  notes TEXT,
  order_index INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 2. Experience 테이블 (경력 사항)
CREATE TABLE experience (
  id BIGINT PRIMARY KEY DEFAULT 1,
  role TEXT,
  period TEXT,
  field TEXT,
  scope TEXT,
  strengths TEXT,
  brands TEXT
);

-- 3. Profile 테이블 (프로필 정보)
CREATE TABLE profile (
  id BIGINT PRIMARY KEY DEFAULT 1,
  hero_title TEXT,
  hero_subtitle TEXT,
  hero_description TEXT,
  about_text TEXT,
  featured_title TEXT,
  featured_subtitle TEXT,
  work_title TEXT,
  work_subtitle TEXT,
  contact_title TEXT,
  contact_subtitle TEXT,
  contact_email TEXT,
  contact_kakao TEXT,
  exp_title TEXT,
  exp_label_field TEXT,
  exp_label_scope TEXT,
  exp_label_strengths TEXT,
  exp_label_brands TEXT,
  about_title TEXT,
  about_subtitle TEXT,
  about_strengths_title TEXT,
  site_name TEXT
);

-- 4. 초기 데이터 넣기
INSERT INTO experience (id, role) VALUES (1, '영상 편집자');
INSERT INTO profile (id, site_name) VALUES (1, 'TEDIO');
```

---

## 4. 이미지 저장소(Storage) 만들기
이미지 업로드를 위해 저장소를 만들어야 합니다.

1. 왼쪽 메뉴에서 **Storage**를 클릭합니다.
2. **New Bucket** 버튼을 클릭합니다.
3. 버킷 이름을 `portfolio`라고 입력합니다.
4. **Public bucket** 스위치를 켜서 활성화합니다 (이걸 안 켜면 이미지가 안 보입니다!).
5. **Save**를 클릭합니다.
